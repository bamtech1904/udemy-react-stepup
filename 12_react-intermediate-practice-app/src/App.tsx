import { Provider } from "@/components/ui/provider";
import { Router } from "@/components/router/Router";

function App() {
  return (
    <Provider>
      <Router />
    </Provider>
  );
}

export default App;
